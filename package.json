{
  "scripts": {
    "test": "firebase emulators:exec 'jest --env=node'"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^2.0.1",
    "firebase": "^9.6.2",
    "jest": "^27.4.7"
  }
}
